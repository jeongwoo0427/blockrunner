# 06. 이동 애니메이션

## 목표

블록이 순간이동하지 않고 미끄러져 보이게 한다. 이 게임의 손맛은 거의 전부 여기서 나온다.

## 선행 조건

- [04-game-screen.md](completed/04-game-screen.md)
- 요구사항: [../game-design.md](../game-design.md) §7
## 작업

### 1. 렌더링 방식 — **04에서 확정됨 (하이브리드)**

바닥·격자선·벽은 `BoardPainter`, 움직이는 블록은 `BoardView` 의 `Stack` 위에 `Positioned` + `BlockTile` 로 얹혀 있다. 각 `Positioned` 에는 이미 `ValueKey(block.id)` 가 붙어 있다.

따라서 이 작업은 **`Positioned` 를 `AnimatedPositioned` 로 바꾸는 것에서 시작한다.** 셀 크기는 `BoardView` 한 곳에서만 계산되므로 좌표계를 새로 맞출 필요가 없다.

주의: 블록 좌표는 `margin + col * cell` 이다. 격자 바깥에 외곽 프레임용 여백이 한 겹 있으므로 **`margin` 을 빼먹으면 판 전체가 어긋난다.** `BoardPainter` 도 같은 값을 `origin` 으로 받는다.

### 2. 애니메이션 구동

`AnimationController`(`SingleTickerProviderStateMixin`)를 **Screen의 State**에 둔다. Notifier는 애니메이션을 모른다 — 도메인 상태만 갖는다.

흐름:

```
MoveRequested
  → Notifier가 MoveResult 계산, state.isAnimating = true, board = 새 상태
  → Screen이 didUpdateWidget에서 board 변경을 감지, controller.forward(from: 0)
  → 진행률 t 로 from → to 사이를 보간해 그린다
  → 완료 시 onEvent(AnimationCompleted) → isAnimating = false
```

**모든 블록이 동시에 출발하고 동시에 도착한다.** 이동 거리가 달라도 소요 시간은 같다 (game-design §7). 거리 비례로 하면 턴 리듬이 흐트러진다.

### 3. 파라미터

`core/config/app_constants.dart`에 모은다.

- 지속 시간 120~180ms (기본 150ms)
- 커브 `Curves.easeOut` 계열
- 실제 값은 손으로 플레이해보고 조정한다. 문서의 값은 출발점일 뿐이다.

### 4. 구멍 낙하 연출

슬라이드가 끝난 **직후** 별도 단계로 재생한다. 축소 + 페이드, 100~150ms.
플레이어가 빠진 경우 연출이 끝난 뒤 되돌리기 유도 UI를 띄운다.

### 5. undo / reset 시의 애니메이션

- **undo**: 역방향 슬라이드로 되돌리면 예쁘지만 구현이 복잡하다. v1은 **즉시 반영(애니메이션 없음)** 으로 하고, 여유가 되면 짧은 페이드만 넣는다.
- **reset**: 즉시 반영.

### 6. 접근성

`MediaQuery.disableAnimationsOf(context)`(또는 `MediaQuery.of(context).disableAnimations`)가 참이면 애니메이션을 건너뛰고 즉시 반영한다. 스킵해도 게임 로직은 동일해야 한다.

## 완료 기준

- [x] 블록이 미끄러지는 것이 보이고, 서로 겹치거나 통과하지 않는다
- [x] 이동 거리가 다른 블록들이 동시에 출발·도착한다
- [x] 구멍에 빠지는 연출이 슬라이드 이후 재생된다
- [x] 애니메이션 중 입력이 무시되고, 완료 후 정상 동작한다
- [x] 애니메이션 비활성 설정에서 게임이 정상 진행된다
- [x] 웹에서 프레임 드랍이 없다 (`-d chrome`에서 육안 확인)

## 열린 질문 — **해소됨**

- ~~벽에 부딪힐 때 짧은 바운스/셰이크~~ → **v1 제외** (사용자 결정). "거리가 달라도 동시 도착" 제약과 충돌한다
- ~~무효 입력일 때 보드를 살짝 흔들기~~ → **v1 제외** (사용자 결정). 무효 입력은 상태를 전혀 바꾸지 않는 것이 규칙이라 알릴 통로를 새로 만들어야 한다

둘 다 기획서 §7 에 제외 이유와 함께 표로 남겼다. 나중에 같은 아이디어가 나왔을 때 대가를 다시 계산하지 않아도 되게 하기 위해서다.

---

## 실제 결과

### `AnimationController` 를 두지 않았다

작업 문서(§2)는 Screen 의 State 에 `AnimationController` 를 두고 진행률 `t` 로 좌표를 보간하라고 했지만, **§1 의 `AnimatedPositioned` 를 쓰면 컨트롤러가 필요 없다.** 좌표 보간은 위젯이 하고, 화면에는 "언제 끝났는지" 를 재는 `Timer` 하나만 남는다. `from`/`to` 를 `BoardView` 까지 내려보낼 필요도 없어져 좌표 계산이 여전히 한 곳에만 있다.

### `isAnimating` 이 "이번 변화를 보여줄지" 를 겸한다

암시적 애니메이션은 값이 바뀌면 무조건 재생한다. 그래서 **다시하기와 레벨 로드가 되감기 연출로 재생되는 문제**가 생긴다. `isAnimating` 이 `false` 면 지속 시간을 0 으로 주는 것으로 해결했다 — 다시하기는 이 플래그를 세우지 않으므로 즉시 반영(기획서 §7)이 공짜로 지켜진다. `07` 의 되돌리기도 같은 방식으로 즉시 반영된다.

### 낙하는 `Interval` 로 순서를 만들었다

암시적 애니메이션에는 시작 지연이 없다. 타이머를 하나 더 두는 대신 **전체 구간(슬라이드+낙하)을 하나로 잡고 앞부분을 `Interval` 로 비웠다**(`AppConstants.fallStartFraction`). 단계 사이를 손으로 이어붙이지 않아도 된다.

축소·페이드 위젯은 **빠지지 않는 블록에도 항상 걸어둔다.** 빠지는 순간에만 감싸면 위젯이 새로 생겨 시작값부터 그려지고, 애니메이션 없이 사라진다.

### 빠진 블록을 상태가 따로 들고 있다

`MoveResult` 가 이미 `from`/`to`/`fellIntoHole` 을 주고 있었다(01·02 에서 "애니메이션이 필요로 하는 정보" 로 넣어둔 것). 다만 `result.board` 에서는 이미 지워져 있어서, 그대로 그리면 **구멍까지 미끄러지지 않고 제자리에서 사라진다.** `GamePlayScreenState.fallingBlocks` 에 빠진 칸 좌표와 함께 담아두고 연출이 끝나면 비운다.

### 결과 오버레이 시점은 화면이 정한다

클리어·소실 판정은 이동 계산 직후 확정된다(Notifier). **보여줄 시점만 화면이 `!isAnimating` 으로 늦춘다.** 판정을 늦추는 방법도 있었지만 그러면 `isCleared` 가 실제 판과 어긋나는 시간이 생긴다 — 언제 그릴지는 표현의 문제이지 도메인의 문제가 아니다.

### 남은 사항

- 클리어 시 **목표 칸 강조**(기획서 §7)는 넣지 않았다. 결과 오버레이가 바로 뜨는 지금은 강조가 보일 자리가 없다. `07-undo-reset` 에서 오버레이를 손볼 때 같이 본다
- 되돌리기 연출은 `07` 의 몫이다. 즉시 반영으로 동작하도록 배선은 이미 돼 있다
